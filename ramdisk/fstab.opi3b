# Android fstab file.
# The filesystem that contains the filesystem checker binary (typically /system) cannot
# specify MF_CHECK, and must come before any filesystems that do specify MF_CHECK

#<src>                                     <mnt_point>  <type>  <mnt_flags and options>                             <fs_mgr_flags>
/dev/block/mmcblk1p2                       /system      ext4    ro,barrier=1                                        wait,first_stage_mount
/dev/block/mmcblk1p3                       /vendor      ext4    ro,barrier=1                                        wait,check,first_stage_mount
/dev/block/mmcblk1p4                       /metadata    ext4    noatime,nosuid,nodev,discard                         wait,check,formattable,first_stage_mount
/dev/block/mmcblk1p5                       /misc        emmc    defaults                                             defaults
/dev/block/mmcblk1p6                       /data        ext4    noatime,nosuid,nodev,nomblk_io_submit,errors=panic  wait,check,formattable
/devices/platform/*.usb/usb*               auto         auto    defaults                                            voldmanaged=usb:auto
